server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://pg-svc:5432/hoteldb
    username: postgres
    password: admin123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


  application:
    name: HOTEL-SERVICE


#  config:
#    import: configserver:http://localhost:8888/